import styled from "styled-components"

const PSStyle = styled.div`

        margin: 20px;
        #digits {
            float: left;
        }
        #redeemBtn {
            float: right;
            top: -49px;
            right: -15px;
        }
    `
export default PSStyle