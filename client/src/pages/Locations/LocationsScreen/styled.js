import styled from "styled-components";

export const Layout = styled.div`
    flex: 1;
    position: relative;

    .view-toggle {
        position: absolute;
        left: 0;
        bottom: 0;
    }
`;