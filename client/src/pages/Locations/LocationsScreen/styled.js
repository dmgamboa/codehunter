import styled from "styled-components";

export const Layout = styled.div`
    flex: 1;
    position: relative;

    .view-toggle {
        position: absolute;
        right: 0;
        bottom: 0;
    }
`;